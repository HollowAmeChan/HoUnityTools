# 眨眼约束（自动眨眼 + 果冻眼）设计

`HoUnityTools/Constraints/Ho Blink Constraint`。一个组件、两个模块：

- **自动眨眼**：程序化生成眨眼曲线，写到网格的眼睑形态键上，同时把值作为内部驱动暴露出去。
- **果冻眼**：读形态键（眨眼键、眼睛左右/上下运动键等），过一遍弹簧-阻尼，映射到用户自建的键（高光位置、眼仁形状、眼睑附加形变……）。

两者共用同一套**规则 / 目标映射**结构，也共用同一套键名引用方式。

## 设计约束（本版按反馈定稿）

- **不做形态键目录/按表查询**。内置一份**写死的键名表**（只覆盖眼睑与凝视），面板里以下拉形式给出，条目显示成 `键名 (规范)`（如 `Blink (VRM1)`、`まばたき (MMD)`）。用户自建的键直接手填文本。
- **键名存储是 `string` + 下拉**：手填与预设走同一套存储，加键不需要改代码。
- **不做左右眼之间的随机差异**，也不做"左右独立眨眼"。眨眼只有一个计时器，输出一个值；"单眼版本 / 双眼版本"指的是**模型上有哪些键**（一个双眼键，还是左右两个键），不是两套独立时序。需要 wink 这类单眼动作时用显式 API。
- **果冻弹簧是每条规则自己的**（一条驱动一套响应）。
- **组件不持有大量整体参数**；每个小映射（目标）自带自己的 ramp 与增益/范围。
- **用户不细调 ramp**：目标上只给"ramp 预设 + 强度"两个旋钮，原始曲线/时间常数收进 `自定义` 档。动态超调（甩一下再回来）**只由规则级弹簧负责**，ramp 只做"这一路的幅度形状与快慢"，避免两个弹簧互相打架（见「目标 ramp」一节）。
- **眯眼/睁大键不占基础流程**：它们照常在内置表里可手选，但**没有预设会自动接线**（要"闭眼顺带眯一下"就自己加一条规则）。
- 只写形态键（`SetBlendShapeWeight`），不碰 Transform、不碰材质，因此和 Animation Rigging / MC2 没有写者冲突。

## 适用范围与非目标

适用范围：

- 任何带 Blendshape 的 `SkinnedMeshRenderer`（人形、兽形、道具眼睛都行）。
- 面捕/动画已经驱动眼睑键时，可以只把它当"果冻眼"用（自动眨眼关掉）。
- 模型只有双眼键、或只有左右键，都要能用。

非目标：

- 不做形态键之间的拓扑运算（不合并形状、不改 mesh）。
- 不做眼睛的 Transform / 材质属性动画（那属于摆锤约束或 shader 参数绑定）。
- 不自己实现面捕/动捕输入；只**读**别人写好的形态键值。
- 不做左右眼独立随机眨眼（见上）。
- 不做口型/表情的通用映射（结构上能复用，但预设只覆盖眼睛）。

## 术语

| 术语 | 含义 |
| --- | --- |
| **网格列表** | 组件写入/读取的目标 `Renderer` 集合（运行时解析出 `SkinnedMeshRenderer`） |
| **形态键 / 键** | Blendshape，Unity 侧取值 `0..100` |
| **内置键名表** | 写死的 `(键名, 规范标签)` 列表：眼睑（闭合/眯/睁大）与凝视（上下左右/内外） |
| **驱动（Driver）** | 一条规则读取的源：形态键值，或内置的 `AutoBlink` 值，或调试用滑杆 |
| **规则（Rule）** | 以"读哪个键"为主体的一条映射；内含一段果冻弹簧和复数个目标 |
| **目标（Target）** | 一条规则里的一个输出映射项：写哪个网格的哪个键、怎么换算、自己的 ramp |
| **ramp 预设** | 目标级的成形方式（幅度形状 + 一阶时间包络），只给"预设 + 强度"两个旋钮，原始曲线收在 `自定义` 档 |
| **基准值** | 本帧我们介入之前，该键在网格上的值（通常是动画/面捕写的） |

## 数据流

```
网格键值 ──读──┐
自动眨眼 ──────┼─→ 驱动值(单极 0..1 / 双极 −1..1) ─→[可选]输入平滑 ─→[可选]弹簧-阻尼
手动滑杆 ──────┘                                                        │
                                                                        ▼
                        目标映射（每条目标自己的 ramp/增益/偏移/范围/权重）─→ 输出值
                                                                        │
                                        先取基准快照 → 覆盖 or 叠加 ────→ SetBlendShapeWeight
```

## 组件与文件结构

| 文件 | 职责 |
| --- | --- |
| `Runtime/Constraints/HoBlinkConstraint.cs` | 组件本体：序列化字段、键索引构建、每帧求值与写回 |
| `Runtime/Constraints/HoBlinkData.cs` | 可序列化数据：`HoBlinkRule` / 枚举（目标用共享的 `HoShapeKeyTarget`） |
| `Runtime/Constraints/HoBlinkSolver.cs` | 纯数学：眨眼相位机 + 弹簧-阻尼（标量），含 `Sanitize` 与 `maxStep` 子步 |
| `Runtime/Constraints/HoBlinkKeyTable.cs` | 写死的键名表：`(键名, 规范标签)`，按语义分组 |
| `Runtime/Constraints/HoBlinkRampPresets.cs` | 目标 ramp 预设：每个预设的形状曲线、时间常数、强度语义 |
| `Runtime/Constraints/HoShapeKeyResolver.cs` | 网格侧解析：键名 → 索引（精确 → 忽略大小写 → 剥 Blender `.\d{3,}` 后缀），缺失清单 |
| `Editor/Constraints/HoBlinkConstraintEditor.cs` | 面板：分区、预设、规则/目标行、实时横条、调试读数 |
| `Editor/Constraints/HoConstraintEditorTheme.cs` | 面板设计令牌：尺寸栅格、色板、文字样式、运行时生成的九宫格/渐变贴图 |
| `Editor/Constraints/HoConstraintEditorControls.cs` | 自绘控件库：分区头、卡片、行栅格、分段胶囊、数字格、迷你滑杆、横条、键名格 |
| `Editor/Constraints/HoKeyNameDropdown.cs` | 键名下拉（内置表 + 当前网格实际存在的键 + `自定义…`） |

## 数据模型

### 目标网格

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `renderers` | `List<Renderer>` | 接受任意 `Renderer`（与 MC2 的 `sourceRenderers` 一致）；运行时解析出 `SkinnedMeshRenderer`，非蒙皮或没有 mesh 的项跳过并在面板里标黄 |
| `collectChildren` | 按钮 | 一键收集子级里所有 `SkinnedMeshRenderer` |

- 每个网格缓存 `Dictionary<键名, 索引>`，在 `sharedMesh` 变化（换装/mesh 重建）时重建。
- 不判断"这个网格属于哪套规范"：键名怎么解析只看**网格上实际有哪些键**；下拉里出现的 `(规范)` 只是给人和预设看的标签。

### 键引用（内置表 + 手填）

- 序列化存储就是一个 **`string keyName`**：任何键都能填，预设与内置表只是填值的来源。
- 面板里这一格是"下拉 + 文本框"：
  - 下拉内容 = 内置键名表（显示 `键名 (规范)`）+ 当前网格上实际存在的键名（命中内置表的在名字后带规范标签，未命中的标 `自定义`）+ `自定义…` 项。
  - 选 `自定义…` 后就是纯文本输入。
- 命中但网格上不存在时，该行标黄，面板顶部汇总"3 个键在目标网格上不存在"。
- 匹配规则（仅为容错，不做语义推断）：精确名 → 忽略大小写 → 剥 Blender 重复后缀（`Blink.001`）。同名键出现在多个网格上时默认全部写入。

### 驱动（规则的主体）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `driverKind` | `形态键 / 自动眨眼 / 手动 / 眼皮速度` | `自动眨眼` = 本帧闭眼量（0..1）；`眼皮速度` = \|Δ闭眼量\|/Δt 归一化后（0..1），只在眼皮动的那几帧有值 |
| `positiveKey` | 键引用 | 正向键（`ShapeKey` 时必填） |
| `negativeKey` | 键引用（可空） | 负向键，用于把一对键合成带符号的轴（如 `LookRight − LookLeft`） |
| `driverRange` | `Unipolar / Bipolar` | 单极：值域 `0..1`；双极：`(正 − 负) / 100`，值域 `−1..1` |
| `invert` | bool | 反相 |
| `readWrittenThisFrame` | bool | 默认关：读**基准快照**（切断自反馈）。打开后按规则顺序读本帧已写入的值，可以自建链式联动；读写同一个键时面板给警告 |
| `missingPolicy` | `Skip / Warn / Error` | 驱动键找不到时的行为 |

### 规则

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `label` | string | 面板标题（默认自动生成，如 `Blink_L`） |
| `enabled` | bool | 整条规则开关 |
| `jelly` | 段 | 这条规则的果冻弹簧参数（见下） |
| `targets` | `List<HoShapeKeyTarget>` | 复数个目标映射项（可以是空的：这条规则只搭结构） |

### 目标映射项

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `label` | string | 空 | **角色**：这一路是干什么的（如「高光 · 压扁」）。纯粹是给人看的标签，显示在目标行最左边 |
| `meshScope` | `全部 / 指定` | 全部 | 写入范围 |
| `meshIndex` | int | 0 | `指定`时的网格下标 |
| `keyName` | 键引用 | — | 输出键 |
| `blendMode` | `覆盖 / 叠加` | 叠加 | `叠加` = 基准值 + 映射结果；`覆盖` = 直接写映射结果 |
| `gain` | float | 1 | 可为负（反相） |
| `offset` | float | 0 | 加性偏置 |
| `outputMin/Max` | float | 0 / 100 | 输出范围（允许负值或 > 100） |
| `clampToRange` | bool | true | 关掉可以做超范围驱动 |
| `weight` | 0..1 | 1 | 与基准/累加结果混合 |
| **`ramp`** | 段 | — | **这个小映射自己的成形方式**（见「目标 ramp」一节）： |
| `rampPreset` | 枚举 | `直通` | `直通 / 柔跟 / 放大 / 缓入 / 慢放 / 阶梯 / 自定义` |
| `rampIntensity` | 0..2 | 1 | 唯一的细调旋钮：按预设定义的语义缩放（放大倍率、时间常数、超调幅度） |
| `rampCurve` | `AnimationCurve` | 线性 | 仅 `自定义` 档出现：驱动值 → 输出值的形状（**允许 > 1**） |
| `rampAttack` | float（秒） | 0 | 仅 `自定义` 档出现：上升时间常数（一阶） |
| `rampRelease` | float（秒） | 0 | 仅 `自定义` 档出现：回落时间常数（一阶） |

- `ramp` 是**每条目标独立**的：同一份弹簧输出可以既驱动"高光位移"（直通、跟得紧）又驱动"眼仁形变"（缓入 + 慢放）。
- `attack/release` 的状态也是每条目标各自维护，互不影响。
- 跨规则写同一个键时：`叠加` 类的贡献相加，`覆盖` 类按规则顺序后写者胜。

## 每帧流程与不变量

1. **构建**（`OnEnable` / 网格变化时）：解析网格 → 解析每条驱动/目标的键索引 → 编译成扁平数组（无每帧字典查找、无 GC）。缺失键汇总成清单。
2. **快照**：一次性读入所有涉及键的当前值（基准值），之后本帧不再读网格。
3. **驱动**：算自动眨眼 → 得到 `AutoBlink` 值；读各驱动键 → 合成驱动值 → 输入平滑 → 弹簧。
4. **映射**：对每个目标算输出值（增益/偏移 → ramp 预设的形状 → 强度 → attack/release 包络 → 范围/权重）。
5. **合并**：按目标键聚合（叠加相加 / 覆盖后者胜），与基准值合成最终值，最后按「合并方式」处理超过 100 的情况（见下节）。
6. **写回**：只在 `|新值 − 上次写入值| > 0.01` 时调用 `SetBlendShapeWeight`；写过的键记入"本帧已写"集合。

### 时序契约

- 默认 **`LateUpdate`**（和摆锤/漂浮一致）：消费者是渲染，写在动画之后才能覆盖/叠加动画；这里没有同帧下游读者。
- 提供 `更新时机` 选项（`LateUpdate / Update / FixedUpdate / 手动`）：只有当**别的约束在 `Update` 里读这些键值**时才需要提前，按 `docs/FOLLOW_CONSTRAINT.md` 的时序契约办。

### 基准快照（重要不变量）

本帧所有驱动读的都是**快照值**，因此：

- 自动眨眼写眼睑键、同时某条规则又读眨眼键时，不会自己喂自己（反馈被切断）。
- 想表达"眨眼时压扁高光"，用 `AutoBlink` 驱动，而不是去读我们自己写的眨眼键。
- 要真正的链式联动，把规则的 `readWrittenThisFrame` 打开，此时规则顺序变成有意义的求值顺序。

## 形态键为什么老是写死 100（合并方式与饱和诊断）

形态键的合法区间是 `0..100`，而一个键上可能同时有好几个写者。求和在 100 处夹断，就会看到"不管怎么调都是 100"。实际有三种完全不同的来源，对策也不同：

| 来源 | 特征 | 对策 |
| --- | --- | --- |
| **单路自己就拉满** | 基准 0 + 我们 100 → 100，没有被削 | 这是"设计如此"。想留余量：调小该路「增益 / 强度」，或把「输出上限」从 100 压到 85~90，或让 ramp 预设别用「放大」 |
| **多路叠加被夹断** | 两路各要 80/70，加起来 150，夹成 100 —— 两路的比例、果冻超调全没了 | 把「合并方式」换成**软饱和**（永远不到 100，超调看得见）或**按比例分配**（两路一起缩、比例不变） |
| **外部基准已占满** | 基准本身 60~100（面捕/动画/VRChat 表情写着），我们再叠就顶死 | 「合并方式 = 按比例分配」会按剩余空间分配；或者该键改用 `Override` 混合模式；也要确认读的是基准快照（默认如此，自反馈已被切断） |

**合并方式**（`mergeMode`，眨眼与注视各自一个下拉）只作用于**我们自己这一路**：`sum = 0` 时输出恒等于基准，绝不会改写动画/面捕写在键上的值。

| 取值 | 规则 | 满值 |
| --- | --- | --- |
| `Saturate`（默认，老行为） | 求和后夹到 0..100 | 请求 ≥ 100 就是 100 |
| `SoftClip` | 80 以上软压缩：`80 + 20·(1 − e^−(x−80)/20)` | 请求 100 → 约 93；渐近但永远不到 100 |
| `Normalize` | 超过剩余空间时各路按同一比例缩小 | 请求 150 → 100，但 80/70 变成 53/47（比例保持） |

**饱和诊断**：写入器每帧记下每个绑定的「基准 / 我们请求的原始和 / 实际写出值 / 是否被削」，面板的调试区把它们列出来：

```
以下形态键已经写满（或我们这一路被合并策略削过）：
· EyeBlink_L　基准 0 + 我们 100 → 100
· EyeHighlight　基准 40 + 我们 120 → 92.6（被削）
```

看到「被削」就是多路叠加或基准占位在打架，按上表换合并方式；看到「基准 0 + 我们 100 → 100」就是单路到顶，属于正常，要余量就压输出上限。

## 关掉规则 / 关掉约束时的清场（硬写覆盖）

没人再写的时候，我们写过的键不会自己回到原样 —— 必须有人**硬写**回去：

| 时机 | 行为 |
| --- | --- |
| 规则被关掉（`Enabled = false`），目标还挂在写入器里 | 求和里这一路变 0，键自然回到基准 —— **前提是基准没被我们自己污染**（见下） |
| 规则被删 / 目标被移除 / 网格列表变化（重建后那个键不再有人写） | 重建收尾时把它**硬写回基准**（不看写入阈值），否则它会永远停在我们最后一次写的值上 |
| 组件被禁用、取消「允许写入」、`ResetState()` / 面板「重置」 | `RestoreWritten()` 把这一轮写过的键**全部硬写回基准** |

关键不变量：**重建不许把我们自己的输出当成外部基准**。写入器跨重建记住"上一轮写过哪些键、写之前的基准是多少"：

- 重建后仍在写的键 → 继承 `ExternalBase / LastWritten`，于是关掉规则那一帧就能算出"基准 + 0"并写回去；
- 重建后不再写的键 → 用记下来的基准硬写清场。

没有这条，任何一次重建（改规则、换网格、改键名）都会把当时的输出冻成新基准 —— 表现正是"规则一关，键还停在那个值上，怎么都不回 0"。

⚠️ 我们**只还自己拿过的键**：没写过的键（`EverWritten` 为假）一个都不碰，不会顺手改掉动画/面捕写下的值。

## 自动眨眼模型

### 状态机（单计时器）

```
等待(wait) ──间隔到──→ 闭合(closing, closeDuration) ──→ 保持(hold, holdDuration)
   ↑                                                        │
   └────────── 间隔(openDuration 结束 → 重新抽间隔) ←── 张开(opening)
```

- 一个计时器、一个输出值；左右眼由**输出键**决定（一个双眼键，或左右两个键拿同一个值）。
- 间隔抽自 `intervalMin..intervalMax`；`指数分布`模式用 `−mean·ln(1−u)`，比均匀分布更像真人。
- 双击：张开后以 `doubleBlinkChance` 概率在 `doubleBlinkGap` 后立刻再眨一次。
- 眨眼曲线：`AnimationCurve`（横轴 = 归一化相位，纵轴 = 闭合量），默认"快闭慢开"；默认 `闭合 80ms / 保持 40ms / 张开 140ms`。
- 长帧安全：一帧内跨多个阶段时按顺序推进，不丢阶段、不卡住；`deltaTime` 异常大时直接跳到"等待"并重抽间隔。
- 外部接管：若"接管键"（默认眼睑键）基准值持续高于阈值（默认 `5`）超过 `0.5s`，自动眨眼暂停 `2s` —— 面捕/动画在眨眼时不要和程序化眨眼打架。
- 对外 API：`TriggerBlink()`（双眼，走完整曲线）/ `TriggerBlink(left01, right01)`（显式左右，供 wink 用）/ `SetAutoBlinkEnabled(bool)`。

### 参数

| 字段 | 中文 | 范围 | 默认 |
| --- | --- | --- | --- |
| `blinkEnabled` | 启用自动眨眼 | bool | 开 |
| `intervalMin/Max` | 间隔下限/上限 | 0.5–20 s | 2.0 / 6.0 |
| `intervalDistribution` | 间隔分布 | 均匀/指数 | 指数 |
| `closeDuration` | 闭合时长 | 0.02–0.5 s | 0.08 |
| `holdDuration` | 保持时长 | 0–0.5 s | 0.04 |
| `openDuration` | 张开时长 | 0.02–0.8 s | 0.14 |
| `blinkCurve` | 眨眼曲线 | AnimationCurve | 快闭慢开 |
| `strength` | 强度 | 0–1 | 1 |
| `doubleBlinkChance` | 双击概率 | 0–1 | 0.15 |
| `doubleBlinkGap` | 双击间隔 | 0.05–1 s | 0.16 |
| `randomSeed` | 随机种子 | int | 0（0 = 用实例 id） |
| `pauseWhenDriven` | 被驱动时暂停 | bool | 开 |
| `pauseThreshold` / `pauseDuration` | 接管阈值/暂停时长 | 0–100 / 0–10 s | 5 / 2 |
| `blinkTargets` | 眨眼输出 | `List<HoShapeKeyTarget>` | 预设填充 |

## 果冻眼模型

**一条规则 = 一条阻尼追踪曲线**，读什么信号由规则决定（形态键 / 眨眼加速度），公式只有这一个：

```
a = ω²(target − x) − 2ζω·v        ω = 2πf      （欠阻尼二阶系统，永远带回弹）
v += a·dt ; x += v·dt
```

- `f`（面板上叫**跟随**）决定"跟得上多快"，`ζ`（**回弹**）决定回弹：`0.2–0.35` 有明显果冻感，`1` 是临界阻尼完全不超调。
- 驱动本身是跳变的（面捕键值 / 眨眼加速度尖峰），弹簧负责把它变成"甩一下再回来"；再往下每个目标自己的 `ramp` 决定这一路怎么成形。
- **不需要"要不要震荡"的开关**：果冻之所以是果冻就是因为它震荡；也不要"模式"开关，规则读什么信号看它有哪些字段就知道。
- 长帧/暂停保护与摆锤求解器同一套约定：`maxStep` + `Sanitize` + 复位时清速度。

| 字段 | 中文 | 范围 | 默认 | 谁用 |
| --- | --- | --- | --- | --- |
| `frequency` | 跟随 | 0.1–20 Hz | 4.0（眨眼 9.0） | 两种规则 |
| `dampingRatio` | 回弹 | 0.05–2 | 0.35（眨眼 0.20） | 两种规则 |
| `inputSmoothing` | 平滑 | 0–0.5 s | 0.03 | 注视 |
| `driverGain` | 强度 | 0–4 | 1.0 | 眨眼 |
| `blinkEnvelope` | 包络 | 0.005–0.2 s | 0.025 | 眨眼 |
| `maxStep` | 子步上限 | 0.002–0.05 s | 0.016 | 两种规则 |
| `jellyEnabled` / `resetOnEnable` | 内部开关 | bool | 开 | 兼容保留，面板不显示 |

## 目标 ramp（成形方式）

ramp 管的是"这一路输出怎么成形"，不负责产生果冻感。

**分工（重要）**：

- **动态超调**（甩过头再回来）**只由规则级弹簧负责**（频率 + 阻尼比，一处调，物理上只有一个来源）。
- **ramp** 只做两件事：**幅度形状**（含 `> 1` 的放大，也就是"超出"的静态版本）与**时间包络**（attack / release 一阶跟随，无超调）。
- 两个旋钮：`ramp 预设` + `强度`。原始曲线与时间常数只在 `自定义` 档出现，避免用户到处细调。

| 预设 | 形状（驱动 → 输出） | attack / release（强度 = 1） | 强度改什么 |
| --- | --- | --- | --- |
| `直通`（默认） | `y = x` | 0 / 0 | 输出倍率（1.0 → ×1，2.0 → ×2） |
| `柔跟` | 缓入缓出 | 60 ms / 120 ms | 时间常数 ÷ 强度（越大越快） |
| `放大` | `y = 1.25x`（可超出目标范围） | 0 / 0 | 放大倍率 = `1 + 0.25 × 强度` |
| `缓入` | 起步慢、后段快 | 120 ms / 200 ms | 时间常数 ÷ 强度 |
| `慢放` | 线性 | 40 ms / 400 ms | release ÷ 强度（越大松得越慢） |
| `阶梯` | 立即跳到 1 并保持 | 0 / 0 | 无（离散形变用） |
| `自定义` | 用户曲线（**允许 > 1**） | 用户填 | 不参与（曲线/时间都归用户） |

- 默认建议：**多数目标用 `直通`**，果冻感交给弹簧；只有"这一路要更夸张"或"要更黏"时才换 `放大` / `慢放` / `缓入`。
- 需要"幅度超出但不要抖动"时用 `放大` 或自定义曲线（静态超出）；需要"甩一下"时回来调规则的阻尼比 —— 两条路各自独立，不会互相抵消。
- `ramp` 曲线允许超过 1，且 `clampToRange` 关掉时输出可以超过目标范围（配合"超出"用法）。

## 参数总表（控制参数预算）

| 分组 | 数量 | 说明 |
| --- | --- | --- |
| 网格 | 2 | 列表 + 收集子级按钮 |
| 自动眨眼 | 15 | 见上表 |
| 果冻（每条规则） | 6 | 频率/阻尼/平滑/子步/开关/复位 |
| 驱动（每条规则） | 6 | 类型、正/负键、值域、反相、本帧已写 |
| 目标（每项） | 14 | 网格范围、键、混合、增益、偏移、范围、钳制、权重、ramp 预设、强度（+ `自定义` 档的曲线/attack/release） |
| 全局 | 3 | 更新时机、编辑模式求值、写入阈值 |

即 **全局 20 个** + **每条规则 12 个** + **每个目标 11~14 个**（`自定义` ramp 才到 14）。典型配置（3 条规则、共 8 个目标）约 110 个序列化字段，但**日常只动两处**：眼皮输出的键、每条目标的 `ramp 预设 + 强度`。

## 内置键名表（写死）

只覆盖眼睑与凝视；表里每条是 `键名 + 规范标签`，下拉显示成 `键名 (规范)`。

### 眼睑

| 语义 | 条目 |
| --- | --- |
| 双眼闭合 | `Blink (VRM)`、`Blink (VRM1)`、`vrc.blink (VRChat)`、`まばたき (MMD)`、`EyeClosed (UnifiedExpressions Blend)` |
| 左眼闭合 | `Blink_L (VRM)`、`BlinkLeft (VRM1)`、`eyeBlinkLeft (ARKit)`、`EYES_CLOSED_L (Meta Movement)`、`EyeBlink_L (PICO)`、`Eye_Left_Blink (VIVE SRanipal)`、`XR_EYE_EXPRESSION_LEFT_BLINK_HTC (VIVE OpenXR)`、`EyeClosedLeft (UnifiedExpressions Base)`、`ウィンク (MMD)` |
| 右眼闭合 | `Blink_R (VRM)`、`BlinkRight (VRM1)`、`eyeBlinkRight (ARKit)`、`EYES_CLOSED_R (Meta Movement)`、`EyeBlink_R (PICO)`、`Eye_Right_Blink (VIVE SRanipal)`、`XR_EYE_EXPRESSION_RIGHT_BLINK_HTC (VIVE OpenXR)`、`EyeClosedRight (UnifiedExpressions Base)`、`ウィンク右 (MMD)` |
| 眯眼 | `EyeSquint (UnifiedExpressions Blend)`、`eyeSquintLeft/Right (ARKit)`、`EyeSquint_L/R (PICO)`、`Eye_Left/Right_Squeeze (VIVE SRanipal)`、`XR_EYE_EXPRESSION_{LEFT,RIGHT}_SQUEEZE_HTC (VIVE OpenXR)`、`LID_TIGHTENER_L/R (Meta Movement)`、`EyeSquintLeft/Right (UnifiedExpressions Base)` |
| 睁大 | `EyeWide (UnifiedExpressions Blend)`、`eyeWideLeft/Right (ARKit)`、`EyeWide_L/R (PICO)`、`Eye_Left/Right_Wide (VIVE SRanipal)`、`XR_EYE_EXPRESSION_{LEFT,RIGHT}_WIDE_HTC (VIVE OpenXR)`、`UPPER_LID_RAISER_L/R (Meta Movement)`、`EyeWideLeft/Right (UnifiedExpressions Base)` |

### 凝视

| 语义 | 条目 |
| --- | --- |
| 双眼 | `LookUp/LookDown/LookLeft/LookRight (VRM, VRM1)`、`vrc.looking_up/looking_down (VRChat)` |
| 上 | `eyeLookUpLeft/Right (ARKit)`、`EYES_LOOK_UP_L/R (Meta Movement)`、`EyeLookUp_L/R (PICO)`、`Eye_Left/Right_Up (VIVE SRanipal)`、`XR_EYE_EXPRESSION_{LEFT,RIGHT}_UP_HTC (VIVE OpenXR)`、`EyeLookUpLeft/Right (UnifiedExpressions Base)` |
| 下 | 同上，`Up → Down` |
| 左右族 | `EYES_LOOK_LEFT_L/R`、`EYES_LOOK_RIGHT_L/R (Meta Movement)`、`Eye_Left_Left/Right (VIVE SRanipal)`、`LookLeft/LookRight (VRM, VRM1)` |
| 内外族 | `eyeLookInLeft/Right`、`eyeLookOutLeft/Right (ARKit)`、`EyeLookIn_L/R`、`EyeLookOut_L/R (PICO)`、`XR_EYE_EXPRESSION_{LEFT,RIGHT}_{IN,OUT}_HTC (VIVE OpenXR)`、`EyeLookInLeft/Right`、`EyeLookOutLeft/Right (UnifiedExpressions Base)` |

⚠️ **左右族与内外族不要混用**：ARKit/PICO 用 `In/Out`（相对眼球），VRM/Meta/SRanipal 用 `Left/Right`（相对头）。合成双极驱动时选同一族的两把键；混族时面板给警告（不做自动换算）。

## 规则只有两种

果冻眼的规则不是"可切换的模式"，而是**一条阻尼追踪曲线**（弹簧-阻尼），区别只在**它读什么信号**。
所以面板上没有模式开关：规则是哪种，看它创建时的来源、以及它显示哪些字段就知道。

| 规则 | 读什么 | 什么时候用 | 面板上显示 |
| --- | --- | --- | --- |
| **注视**（`＋ 四向注视` 生成） | 形态键：一个键（单极），或"键 / 反向键"两根合成双极 | 高光 / 眼仁跟着视线甩 | 键、反向键、跟随、回弹、平滑、驱动横条 |
| **眨眼**（`＋ 眨眼追踪` 生成） | **眨眼加速度**：组件从眨眼曲线自己算，已归一化 | 眨眼那一下的果冻 —— 大部分情况下只开这一条 | 信号横条、跟随、回弹、强度、包络 |

- 追踪曲线**永远带回弹**（欠阻尼二阶系统）。不需要"要不要震荡"这种开关：果冻之所以是果冻就是因为它震荡。
- 单极 / 双极也不给开关：**反向键留空 = 单极，填了 = 双极**，面板自动切换。
- 旧的 `闭眼量` / `眼皮速度` 信号还留在枚举里（老场景的序列化数据不能坏），只是预设不再生成。

### 参数就这几个

| 规则 | 参数 | 默认 | 含义 |
| --- | --- | --- | --- |
| 注视 | 跟随 `Hz` | 4.0 | 跟得多快 |
| | 回弹 `ζ` | 0.35 | 0.2–0.35 有明显回弹，1 是临界阻尼完全不超调 |
| | 平滑 `s` | 0.03 | 输入平滑，压面捕抖动 |
| 眨眼 | 跟随 `Hz` | 9.0 | 眨完抖几下 |
| | 回弹 `ζ` | 0.20 | 越小抖得越久 |
| | 强度 | 1.0 | 归一化加速度的整体缩放 |
| | 包络 `s` | 0.025 | 把加速度尖峰摊成"瞬间起峰 + 按这个时长衰减"的小鼓包 |

## 眨眼加速度为什么这么算

加速度是这一路的灵魂，但它有两个坑，不处理就没法用：

1. **量纲**：`a = 曲线''(t) / 闭合时长²`。默认曲线在闭合起点的 `|y''| ≈ 4.9`，闭合 80 ms 时真实量级是 **~760 /s²** ——
   随时长和曲线形状漂移。所以按**曲线自身的最大 |y''|**（构建期采 64 个点量一次）归一化，
   这样不管曲线改成什么形状、闭合时长调多少，峰值都稳定在 1 附近，"固定范围"才成立。
2. **冲量太窄**：尖峰只有闭合时长的 ~10%（80 ms → 约 8 ms）。二阶系统对冲量的峰值响应 ≈ `ω·I`，
   9 Hz 弹簧对 8 ms 冲量只有 ~0.5 的响应，而且随频率剧烈变化 —— 参数会变得极难调。
   所以每条规则再各自把它摊成「**瞬间起峰 + 按 `包络` 衰减**」的小鼓包（`blinkEnvelopes[i]`，每规则一份）。

加速度在闭合起点、张开起点各有一个尖峰，中间（保持段）是 0：弹簧被踢两下，中间自由振荡 ——
这正是"眨眼时弹一下、睁眼时再弹一下"的手感。

## 预设

预设只做两件事：**搭结构**（哪条规则、哪些目标、各自的角色标签与 ramp / 混合 / 增益）和**给驱动键**。
它**不猜用户自建的键名**：目标键一律留空，由用户在面板上选。

| 按钮 | 生成 | 参数起点 |
| --- | --- | --- |
| `＋ 四向注视` | 4 条注视规则（上 / 下 / 左 / 右），每条 1 个目标，角色 = 往上 / 往下 / 往左 / 往右 | 4 Hz、ζ 0.35、平滑 30 ms、ramp `直通`、目标 `叠加` |
| `＋ 眨眼追踪` | 1 条眨眼规则 + 4 个目标（高光 · 压扁 / 拉宽 / 下移 / 眼仁 · 压一下） | 9 Hz、ζ 0.20、强度 1.0、包络 25 ms；压扁 `放大` 1.35 + `覆盖`、拉宽 增益 −0.5、下移 增益 −0.45、眼仁 `缓入` 0.9 |

- **驱动键只在网格上真的存在时才填**，找不到就留空并在 Console 说明（不把内置表的键名硬填进去）。
- 追加语义：**点了就加一份，不清空、不重建**；再点一次会再来一份（多了用规则右上的 `⋮` 删）。
- `＋ 空规则` 加一条空白规则（默认读形态键，参数回到出厂值）。

### 目标键怎么选（不猜名字，靠 UI）

每个目标一行，最左边是**角色**（这一路是干什么的，预设给的或你自己写的），接着是键名格：

```
高光 · 压扁   [选键…          ] [▾] ○     ← 还没选键：占位符 + 主色 ▾ + 橙色空圈
高光 · 压扁   [EFT-highLightShrinkZ] [▾] ●1 ← 选好了：绿点 + 解析到的绑定数
```

- 空着的键名格显示「选键…」，右边的 `▾` 画成主色按钮，状态点是橙色空圈 —— 一眼看出"这里要干活"。
- `▾` 打开的是**网格上真实存在的键**（按名字排序，标出"出现在几个网格上"、"已在用"），
  下面再按语义分组列内置键名表作为"标准命名"的参考。带搜索框，直接打字过滤。
- **已在用**的标记很重要：同一个键被两路目标同时写，多半不是你想要的。
- 规则区的摘要会写「N 个目标待选键」，折叠着也知道还剩几个没接。

## 面板结构

面板按 **`docs/EDITOR_UI_SYSTEM.md` 的设计系统**重画（栅格 + 自绘控件，设计稿 `.design/blink-panel.html`）：
所有行左对齐、参数定宽、读数用 54px 横条挂在行尾。
横条里 **色条 = 弹簧/平滑之后真正拿去用的值**，**白刻度 = 弹簧之前的原始值**。

```
Ho 眨眼约束              写入 开 │ 眨眼 开              3 网格 · 12 绑定 · 2 规则
▾ 目标网格                                     3 网格 · 12 绑定
    网格 [Face, Body, Brow…]         [收集子级] [重新解析]
    更新时机 (LateUpdate│Update│Fixed)
    [✓] 允许写入   阈值 [0.1]   合并 (夹断│软饱和│按比例)
    [✓] 编辑模式求值   （不播放时读数不动，除非打开它）
▾ 眨眼                                         开 · Blink_L 等 2 个
                                  [自动匹配眼睑键] [＋ 键]
    [✓] 自动眨眼                                    闭眼 [███░░] 0.42
    ┌ 键 [Blink_L ▾] ●2   [███░░] 42.0   ▸细节  ✕
    │ 范围 (全部│指定)   混合 (叠加│覆盖)   增益 [1.0]
    │ ramp [直通 ▾]   强度 [1.0]   权重 [===|===]
    └ 细节: 通道 / 偏移 / 钳制 / 输出上下限 / 自定义曲线
    间隔 (指数│均匀) [2.0] – [6.0] s
    闭合 [0.08]   保持 [0.04]   张开 [0.14]
    ▸ 细节    曲线 · 强度 · 双击 · 随机种子 · 外部接管
▾ 规则                              5 条规则 · 4 个目标待选键
    [＋ 四向注视] [＋ 眨眼追踪]
    ────────────────────────────────────────────────
    ┌ ▾ 注视 · 上                                ⋮   [✓] 启用
    │   键 [eyeLookUpLeft ▾] ●1   反向键 [选键… ▾ ○]      [██│░░] 0.31
    │   跟随 [4.0] Hz   回弹 [0.35]   平滑 [0.03] s
    │   ▸ 细节
    │   目标 · 这条规则写哪些键                     [＋ 目标]
    │   ┌ 往上 [选键… ▾ ○]   [██░░] 63.2   ▸细节  ✕
    │   │ 范围 (全部│指定)   混合 (叠加│覆盖)   增益 [1.0]
    │   └ ramp [直通 ▾]   强度 [1.0]   权重 [===|===]
    ├ ▾ 眨眼追踪                                ⋮   [✓] 启用
    │   信号 [███│░░░] 0.62
    │   跟随 [9.0] Hz   回弹 [0.20]   强度 [1.0]   包络 [0.025] s
    │   ▸ 细节
    │   目标 · 这条规则写哪些键                     [＋ 目标]
    │   ┌ 高光 · 压扁 [EFT-highLightShrinkZ ▾] ●1   [██░░] 88.0   ▸细节  ✕
    │   └ 高光 · 拉宽 [选键… ▾ ○]   [░░░░] 0.0   ▸细节  ✕
    └ ▸ 注视 · 下                                ⋮   [✓] 启用
                                  [＋ 空规则]
▾ 调试                                 网格 3 · 规则 2
    闭眼 [███░░] 0.42   速度 [█░░░░] 0.12    相位：等待下一次眨眼
                                     [眨一次] [wink 左] [重置] [清空]
    ────────────────────────────────────────────────
    [✓] 手动驱动
    注视 · 上        [██│░░] 0.31   [====|===]
      EFT-highLightShrinkZ  [██░░░] 63.2
    眨眼输出
      Blink_L       [██░░░] 42.0
```

- **所有分区默认折叠**：打开面板先看摘要（摘要里有键名、条数、开关状态），要看哪块点哪块。
- 顶部一行标题：组件名 + `写入 / 眨眼` 状态胶囊 + 右侧计数；没有全局工具栏。
- 一条规则展开后是 **3 行**（键/信号 + 驱动条 / 跟随·回弹·平滑 或 强度·包络 / 细节），一个目标是 **3 行**（最左边那个标签就是目标的角色）；`⋮` 里是上移 / 下移 / 删除。
- 少选项（≤4）一律用**分段胶囊**直接点，不做下拉；`ramp 预设`（7 档）才用菜单。

### 实时横条

数值一律画成条，不再靠一串小数点去猜"现在到哪了"：

| 位置 | 画什么 | 条的范围 | 白刻度 |
| --- | --- | --- | --- |
| 规则内 / 调试区每条规则 | 驱动值 | 单极 `0..1`、双极 `−1..1`（从中轴往两边长） | 弹簧/平滑**之前**的原始值 |
| 每个目标（规则内 + 调试区） | 最终写进键的值 | 该目标的 `输出下限..输出上限` | 无 |
| 眨眼区 / 调试区 | 闭眼量 | `0..1` | 无 |
| 调试区 | 眨眼输出每个键 | `0..100` | 无 |

- 条只有 **54px 宽**，右边紧跟数值 —— 它是"顺手看一眼"的指示器，不是主角，不铺满整行也不单独占行。
- 双极量的条从**中轴**往两边长，正负一眼可见；单极量从左往右长。
- 白刻度与色条分开看，就能判断"是输入本来在动，还是弹簧把它拖成这样"（超调时白刻度会明显落后于色条）。
- 条只在**播放中 / 打开「编辑模式求值」/ 打开「手动驱动」**时才会动：前两者由组件自己每帧求值，面板在播放或手动驱动时额外 `Repaint()`。

### 交互细则

- **规则卡片**：折叠头只写名字（驱动键在下面那行键名格里看得见），右侧 `⋮` 菜单 + `启用` 勾选。
- **正 / 负键一行**：`正 [键 ▾] ●N` 与 `负 [键 ▾] ●N` 并排，行尾跟驱动横条；单极只填「正」。驱动不是"形态键"时整行禁用。
- **键名格**：`[键名] [▾] [●N]`。`▾` 打开键名菜单（内置表 + 当前网格实际存在的键 + `自定义…`）；状态点绿 = 解析到的绑定数、红叉 = 这些网格上没这个键、短横 = 没填 —— 一眼看出"到底在写谁的键"。
- **不常动的项进「细节」**：眨眼区是曲线 / 强度 / 双击 / 随机种子 / 接管判定；规则是反相 / 读本帧已写值 / 子步上限；目标是通道 / 偏移 / 钳制 / 输出上下限 / 自定义曲线。
- **缺失键**：目标网格卡片里汇总 `HelpBox`，附「重新解析」按钮。
- **多选编辑**：同 `HoPendulumConstraint`，多实例选中时批量应用预设。

### 调试视图

- 一行：闭眼量横条 + 相位文字（等待 / 闭合中 / 保持 / 张开中）；计数在分区头右侧。
- 「眨一次 / wink 左 / 重置 / 清空」四个按钮，不用真等自动眨眼。
- **每条规则**：名字 + 驱动横条（白刻度 = 原始）+（手动驱动时）迷你滑杆；**每个目标**再跟一条输出横条，标题就是键名 —— 不展开规则也能看数值怎么流。
- 不在播放、也没开「编辑模式求值」时，给一行小字提示读数为什么不动。
- **饱和清单**：本帧写满 100 或被合并策略削过的键（键名 + 基准 + 我们的请求 + 最终值），见「形态键为什么老是写死 100」。

## 调试工作流

| 现象 | 先看什么 | 常见原因 |
| --- | --- | --- |
| 完全不眨眼 | 调试区驱动值；缺失键汇总 | 眨眼输出键没解析到（键名拼错/网格没放进来） |
| 眨眼了但动画也在眨 | 目标的混合模式与基准值读数 | 用了 `覆盖` 覆盖动画；改 `叠加` 或打开"接管判定" |
| 果冻感太硬 / 不回弹 | **规则的阻尼比、频率** | ζ 太高（≥0.7）；或输入平滑太大把阶跃抹平了 |
| 幅度不够 / 过了 | 目标的 `ramp 预设` 与强度 | 该换 `放大`；或强度不等于 1 |
| 高光跟不满 / 过冲 | 目标增益、值域（单极/双极） | 单极/双极选错；`In/Out` 与 `Left/Right` 混族 |
| 抽搐/抖动 | 驱动键原始值读数 | 面捕键值本身抖动 → 加输入平滑；或 spring 频率过高 |
| 眨眼"拖泥带水" | 目标的 `ramp 预设`（换成 `直通`）与强度 | 用了 `慢放`/`缓入`；或强度的语义被误解（时间常数 ÷ 强度） |
| 停帧后爆掉 | `maxStep` | 长帧没有子步（对齐摆锤的长帧处理） |
| 只有一半网格动 | 网格列表与 `meshScope` | 该网格缺这个键（缺失键汇总里能看到） |
| 形态键总是 100，怎么调都一样 | 调试区的饱和清单 | 见「形态键为什么老是写死 100」：单路到顶（压增益/输出上限）／多路叠加被夹断（换软饱和或按比例分配）／外部基准占满（按比例分配或改 `覆盖`） |
| 果冻的超调看不见 | 目标是不是已经写满 | 输出被夹在 100：把「输出上限」压到 85，或把「合并方式」换成软饱和 |

## 性能与资源约束

- 构建期完成全部键解析；每帧只做数组遍历，无字典/无 LINQ/无分配。
- 阈值写 + 上次值缓存；同一个键每帧最多写一次。
- 规则/目标编译成扁平数组（规模是几十个量级，托管数组即可）。
- `sharedMesh` 变化才重建索引；`OnDisable` 释放缓存。
- 调试字符串只在面板可见时生成。

## 里程碑与验收

1. **键名表 + 解析器 + 键下拉**：给一个真实模型，下拉里能同时看到内置条目和该模型实际存在的键，并标出哪些内置键在模型上不存在。验收：ARKit 型（只有左右）与 VRM1 型（有 `Blink`）都能选对。
2. **自动眨眼 + 眨眼输出**：程序化眨眼可见、可调、可在编辑器预览；双眼键版/左右键版两套预设可用。验收：闭合时长/间隔统计符合参数；被面捕驱动时自动让位；左右两眼的值始终一致。
3. **规则列表 + 果冻弹簧 + 目标 ramp**（核心）：高光/眼仁联动可用。验收：驱动阶跃时输出有可调的超调与回落（超调来自规则弹簧）；同一条规则下两个目标可以用不同 ramp 预设（一个 `直通`、一个 `缓入`）；只动"预设 + 强度"就能把效果调到可用；静止时收敛不漂移。
4. **预设**：眼睑键自动匹配（双眼键版 / 左右键版）+ 规则两颗追加按钮（四向注视 / 眨眼追踪）。不做"顶栏一键装配"，也不猜用户自建的键名：目标键由用户在面板上从"网格上真实存在的键"里选。
5. **调试视图 + 文档实测数据**：把长帧、面捕抖动、混族等场景的实测结论补进本文档。验收：规则/目标的实时横条能看出"原始 → 弹簧 → 最终键值"这条链路。
6. **文档回填**：`ramp 预设表`里"强度改什么"的数值要按实际手感复核一遍（现在写的是设计意图，不是实测值）。

## 待确认

1. **动态超调只在规则弹簧里，目标 ramp 只做"幅度形状 + 一阶时间包络 + 强度"** —— 认可吗？如果你希望单个目标也能自己甩（比如高光要甩、眼仁不要甩，但两者在同一个驱动下），我就把 `自定义` 档升级成"带一个小二阶包络"，代价是同一路可能出现两个弹簧叠加。
2. **`ramp 预设` 的档位**：现在是 `直通 / 柔跟 / 放大 / 缓入 / 慢放 / 阶梯 / 自定义`。够用还是要合并几个（例如去掉 `柔跟`，让 `缓入` 兼）？
3. **`TriggerBlink(left01, right01)` 这个显式 wink 入口**要不要保留？（不做随机单眼，但手 K 剧情 wink 可能用得上）
4. **更新时机默认 `LateUpdate`** 确认（消费者是渲染，写在动画之后才能叠加；若以后有别的约束在 Update 读这些键值再提前）。
5. **预设落地形式**：写死在代码里（`HoBlinkPresetActions`），还是做成可扩展的 `ScriptableObject` 资产？建议先写死。
