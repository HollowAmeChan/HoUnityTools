# 动态参数（语义输出）：Hub + 前缀

2026-09-25 定稿；**2026-09-26 ~ 27 连着改了五次设计**，最后落成现在这个形状：
**一个组件**（`HoFaceSemanticHub`）、**一条键值对列表**、**一个写入口**（中间层）。
⚠️ 中间那几版（ScriptableObject 词表 / 曲线写 `values.<i>` / 控制器状态上的写手 / 槽表 / 「把手」Connector）
**都删了**；"为什么留不住、当时咬了我们什么"收在
[踩过的坑 · 动态参数](pitfalls/FACE_TRACKING_DYNAMIC_PARAMETERS.md) ——
**本文只写现在的形状**。
来源：用户与 GPT 的《Unity控制器链式用法》调查（导出稿）+ 本仓库现状核对。
**这份文档只讲"语义参数"这一条新通道**；面捕的输入侧（线名 → 规范名 → 曲线）仍见
[面捕中间层处理](FACE_TRACKING_MIDDLE_LAYER.md)。

> ⚠️ 本文是**设计 + 未验证清单**。代码已落的是**一个类型**（`HoFaceSemanticHub`，
> 两边程序集各一份、同一个实现、走 `.research/sync-modcore.ps1` 同步，**同步清单共 9 份**）。
> **Unity 侧那条链现场跑通过**（跑的是当时的形状，现在的形状见 §5.2 的待办）；
> **Warudo 侧还没跑过**（节点连线 + 面板）。
>
> ✅ **已经实测掉的**（2026-09-25）：
> ① `MonoBehaviour` 与 `ScriptableObject` **能在 mod 程序集里编译、且 lint clean** ——
>    `docs/FACE_TRACKING_WARUDO_ROUTE.md` 那条"不支持 ScriptableObject"说的是
>    "**UMod 不替你加载 `.asset` 资源**"，不是"这个类型不能出现"；
> ② **带自定义脚本的 Warudo 角色 mod 打包是既有能力**（FastBuild 把源码复制进包、UMod 编译，
>    另有产物校验器专门查"复制的脚本有没有进程序集"）—— 用户确认"这条路绝对 ok"；
> ③ 这些类型同步进 mod 之后，mod 侧 `compile-check` 通过（lint clean）。

## 1. 为什么要有它

`AnimatorController` 只是**动画资产控制器**：它吃参数、吐**动画姿态**。它天生**没有"参数输出"**，
所以"控制器 A 的输出喂给控制器 B"在 Unity 里没有现成机制。

解法不是把控制器改成万能角色控制器，而是给它加一条**很薄的第二输出**：

```
Face Capture → 中间层（*.hoface.json：输入行 / 曲线 / 输出行）
                 ├─→ 控制器参数 → Animator Controller → 动画姿态（形态键 / 骨骼）
                 └─→ 动态参数（**同一份字典**）→ Dynamic Parameter Hub
```

于是：

* **控制器只干动画**（形状、骨骼，将来也许材质）；
* **动态参数由中间层算完顺手写进 Hub**，任何脚本 / 材质 / 蓝图读 Hub 就够；
* 换面捕方案（ARKit → 手机 → 手动 → 另一个控制器）时，**消费方一行都不用改**。

⚠️ 注意上图那条"中间层"的分叉：**Hub 里的东西不是控制器吐出来的，是中间层算出来的那份参数**
（Unity 侧会话算完写，Warudo 侧「HoFace写动态参数」节点算完写）。

## 2. 三个角色，各管一件事

| 角色 | 是什么 | 负责 |
|---|---|---|
| `HoFaceSemanticHub`（**唯一的组件**） | MonoBehaviour，挂在角色 mod 的一个空物体上 | **这一帧是多少**：`List<HoFaceSemanticSlot> parameters` —— **一格一个 `(名字, 值)` 键值对**。按名字读/写（`GetFloat(名字)` / `SetFloat(名字, 值)`，没有这一格就当场开一格）⇒ 没有长度政策、没有下标约定、**名字也只活在运行期** |
| **写的人 = 中间层** | Unity 侧：调试会话（`HoFaceAnimationSession.PublishSemantics`，**可选开关、默认关**）；Warudo 侧：「HoFace写动态参数」节点（`HoFaceHubWriteNode`） | **把算出来的参数按名字写进 Hub**。名字就是配置文件里输出行的 `parameter`（§5） |
| 消费方 | 作者的脚本 / 材质控制 / 蓝图 | 拿 **Hub** 那个引用，按名字读（`hub.GetFloat("MouthOpen")`）—— **先跑一遍看 Hub 里有什么名字，或者两边事先约定** |

⚠️ **Hub 不限制写者 —— 它是一片"脚本写的工作台"。**（2026-09-26 用户纠正过一次：
别把它说成"只有中间层能写"）我们的中间层是**我们的**写者（上面那一行那两条入口）；
作者自己挂在角色上的组件、别的 mod 的脚本，同样可以 `SetFloat(名字, 值)` 按名字写进来 ——
那正是这个组件给的入口。
**唯一写不进去的是动画 clip**。
换句话说，"Hub 里能有什么"是**产品边界**（我们提供中间层这一条），不是**机制边界**。

### 2.1 为什么只有一个组件、一条键值对列表（2026-09-27 用户定）

**一个组件**：东西只有"值"和"按名字读写它"两件事，所以值、名字、入口全在 Hub 自己身上。
当年在它旁边还挂过一个「把手」（`HoFaceSemanticConnector`）转一手，**删掉的理由见坑档 §4**
—— 它只剩"再指一次 Hub"，还多两个失败模式（忘挂、没填引用）。

**一条键值对列表**（不是两个平行数组）：
老形状是 `float[] values` + `string[] names` **按下标对齐**，那套不变量只为"曲线写下标"服务，
而那条路已经没了 ⇒ "两个数组必须等长、下标必须对齐"成了**纯粹的下标漂移风险**。
现在 `List<HoFaceSemanticSlot>` 一格一个 `(key, value)`，**没有平行不变量可以破坏**。

**为什么值不是一堆 `public float MouthX`**：三个理由缺一不可 ——
① 运行期读**不能反射**（UMod 禁 `System.Reflection`），字段名读不出来、按名字查表可以；
② 参数集合是**每份配置自己定义的**、不是编译期固定的 ⇒ 字段名没法在编译期写出来；
③ 加一个参数 = 中间层多一行输出，**两边代码一行不动**。

⚠️ **"名字对不上"没有校验点**（表删掉的代价，认了）：配置里把 `MouthX` 敲成 `Mouthx`，
那条值会**安静地**落到另一个槽（`Mouthx`），而读 `MouthX` 的人只会拿到 0。
发现它的办法只有两条：**跑一遍看 Hub 里的名字**（或面板的「参数输出」栏），
或者看节点/会话状态里那行**新声明**（新开出来的名字会点名）。
⚠️ **中性值不在任何表里**：中性 = `0`，是**静态规则**（想清就 `hub.Zero()`）。
`0.5` 那种"中立位"是 VBridger 私有的约定，我们**不采用**（[HO 参数规范](PARAMETER_HO.md) §2）。

## 3. 命名前缀：只给"生成预制动画"那一步用

控制器生成器（把参数定义编译成混合树 + 叶子 Clip）需要知道**每个槽该写哪里**。用名字前缀表达：

```
SK/xxx   → SkinnedMeshRenderer 的 BlendShape
BT/xxx   → Bone / Transform 动画
P/xxx    → 动态参数（写进 Hub）
```

例：`SK/MouthSmile`、`BT/Jaw`、`P/MouthX`。

**⚠️ 前缀只存在于生成 IR / 参数定义里，生成完的控制器不需要知道它。**
这条很重要，而且它跟我们既有的另一条规矩是同一条：

> **解释控制器结果时绝不看名字。**

我们的驱动路径（喂参数 → 跑影子 → 采结果）**本来就是按结构走的**：骨骼按 `HumanBodyBones` 枚举、
形状按网格上的 `GetBlendShapeName(i)` 枚举。名字只在**生成**与**中间层**那两步参与。

⚠️ **一处例外要说清**：形状采集是"按名字"取权重的（下标来自 `mesh.GetBlendShapeName(i)`）。
但那是**网格上的形状名**，与"控制器参数名的前缀"完全是两回事 —— 前者是 Unity 的绑定机制，后者只是生成期的协议。

## 4. 落点约定

* **Hub 挂在角色子层级的一个空物体上**（不是塞进角色模型层级里），例如 `Character/SemanticHub`。
* **写的人是中间层，不是控制器**：Unity 侧不需要任何人在控制器上 `Add Behaviour`；
  Warudo 侧不需要把求解节点接到角色上（那是「HoFace写动态参数」节点的事）。
* **两边不需要约定顺序，也不需要一张表**：写的人按**名字**开格（没有就当场开）并写下名字
  ⇒ 顺序完全由"谁先声明"决定，没有任何东西会因为重排而错位。
* 消费方拿 **Hub** 那个引用：`GetFloat("名字")`（每帧都要读同一个名字的话，自己缓存一次下标更省）。
  **想知道有哪些名字：跑一遍看 Hub（面板/Inspector），或者两边事先约定。**
* **没有跨角色共享的词表**：名字由**每份配置的输出行**决定，一个角色一份。改一处不影响别的角色。

### 4.1 "槽位预留多少"这个问题**不存在了**

**格数 = 中间层输出行的行数**，写的人自己按名字开格：没有魔法数字、没有长度政策、
也没有"作者必须预先知道下标"。Hub 的 `parameters` 出厂就是**空列表**。
（老形状要预留 128 个槽、还悬着三个候选政策的经过，见[坑档](pitfalls/FACE_TRACKING_DYNAMIC_PARAMETERS.md) §1/§3。）

## 5. 中间层怎么把值写进 Hub

**两条写入口，写的是同一份东西**：

| 谁 | 怎么写 | 名字从哪来 |
|---|---|---|
| **Unity 侧**：调试会话 | **可选开关**（调试设置里的 `writeParameterHub`，**默认关**）：打开后每帧算完输出行就 `hub.SetFloat(parameter, value)`（没有那一格就当场开一格） | 配置文件里**输出行的 `parameter`** |
| **Warudo 侧**：「HoFace写动态参数」节点 | 每帧把 `参数处理.参数`（或「HoStringFloatMerge」的 `字典`）按名字写进角色 Hub | 同一份字典的**键** |

⚠️ **Unity 侧默认关**（2026-09-26 用户定）：Hub 是"把值挂出去给**别的**脚本 / 材质 / 蓝图读"的出口，
**调试台自己不需要它** —— 面板的「参数输出」栏看的就是同一份值；Warudo 侧也不靠它。
所以默认不往外写东西，需要时在面板「对象」段把「写动态参数 Hub」打开（下一帧生效）。

写的是**全部输出行**（不按"控制器里有没有这个参数"过滤）—— 面板的「参数输出」栏摊开的就是这一份，
所以"面板里看到什么 = Warudo 里是什么"这条规矩在动态参数上也成立。
（Unity 侧面板「参数输出」栏每行右边那四个**覆盖按钮**（不覆盖 / -1 / 0 / 1）盖掉这一行时，
Hub 里也跟着是那个值 —— 覆盖走会话的 `SetPreview`，它在所有生产逻辑之后生效，
所以"面板 → 影子树 → 角色 Hub"三段看到的是同一份。调试按键 / 门控就靠它。）

### 5.1 现场与回归门

* ✅ **Unity 侧现场跑通过**（一次，老形状：写手 → 影子 Hub → 中继，记录见坑档 §6）。
* ⚠️ **现在的形状（中间层 → 角色 Hub）还要重跑一遍现场**：判据 = Hub 里有名字与实时值。
  机器化的回归门是 `Tests~/FaceTrackingValidation.cs` 的 `stage 18`：会话算完一帧后断言角色 Hub 上
  **槽数 = 输出行数**、且 `ARKit/jawOpen` ≈ 0.6（`HO_HUB_PUBLISH` 那一行是它的读数）。
* ⚠️ **Warudo 侧的连线与面板还没验**（节点 → 角色 Hub 真的写进去了没有）。
  Unity 侧的现场与批处理用例都测不到 Warudo 的节点行为 —— 那是跨仓验证项
  （[仓库与提交](pitfalls/REPO_AND_GIT.md) §7）。

### 5.2 Hub 不是"控制器的中间值"，那"控制器内部才有的量"怎么办

"控制器内部才有的量（读姿态 / 取决于当前状态的值）中间层看不到"这条需求**没有被实现**，
也**没有借口用 Hub 兜** —— 但要把话说准：**不是"Hub 写不了"**，
而是"**我们不再提供跑在控制器里的写者，clip 也写不了 Hub**"。想要的人可以自己写一个组件
（读 Animator 参数 / 姿态，算完按名字写 Hub）—— 那正是"脚本工作台"允许的事，只是不在我们的产品范围内：

* **姿态**（形态键权重 / 骨骼旋转）**本来就能采**：求解节点一直在采（`HoFaceController.BlendShapes` /
  `BoneRotations`），只是它采的是"控制器算出来的姿态"，而不是"喂回去的参数"。
  真要"按姿态再产出一个参数"，正确的位置是**中间层再来一遍**（它已经能读输入行与通道），
  而不是往控制器里塞写者。
* **作者根本不用我们的中间层**（自己画树、自己管参数）时，我们这条链**不适用** ——
  这正是"我们的写者只有中间层"的代价，认了（用户 2026-09-26 的选择：**中间层全部算完，顺手输出到 Hub**）。
  那种场景下作者自己的脚本照样能写 Hub，我们只是不替他算。
* **取决于当前状态 / 层权重的值**：Warudo 侧另有官方的 `Get Animator …` / `Get Character …` 节点族，
  Unity 侧有脚本 —— 不该由这条链承担。

#### ⚠️ 唯一真会撞上的那条：**树里的"有效门控"**

"控制器内部才有的量"里，实际只有一件事会撞上来：**混合树里乘出来的有效门控**。
一层门控（一个区域一个参数）时"有效门控 = 那个参数"，Hub 里传的就是真值、下游不用重算；
**嵌套 / 乘法分叉**时有效值只活在树里 —— 我们的两条入口（中间层 / clip）都交不出来，
要就得自己写个组件去读参数、算完写 Hub。
所以定了一条边界：**树 gate 只做一层**；要"两个开关的与"就在**中间层**乘好，作为一行输出喂进树。
完整论述与现在这份控制器的形状见
[控制器结构](FACE_TRACKING_CONTROLLER_STRUCTURE.md) §3.3；引擎能力本身见
[混合树能力边界](BLEND_TREE_LIMITS.md) §2 与 [踩过的坑 · 混合树](pitfalls/BLEND_TREE_TRAPS.md) §3。

### 5.3 Hub 是"求值帧"里语义那**一格**，不是全部

把控制器的求值结果摊开看，`signals`（导出语义 = 写进 Hub）只是其中一格；
形态键与骨骼走曲线绑定，**其余几类（显隐 / 组件档位 / 材质 / 对象引用）我们不做** ——
那是 [控制器输出范围与实测能力](ANIMATOR_OUTPUT_PIPELINE_DESIGN.md) §6/§7 收窄掉的东西。
所以**不要**把显隐、材质、引用这些也塞进 Hub：Hub 的槽是 **float**，
塞进去就得自己定编码（0/1 当真假、三个槽当一个向量），而且会把 Hub 变成万能写入口。
**本文件只负责 `P/` 这一类（语义信号）。**

## 6. 我们**不做**的事

* ❌ **不写组件值 / 材质值**（原先规划里那一条）。理由：它们的**能力上限取决于我们能不能读回来**，
  而读回来要么反射（禁）、要么需要一个能被动画的已知类型 —— 成本远高于收益。
  语义值走 Hub 已经覆盖了"下游要读"这件事。
* ❌ **不让控制器碰动态参数**（2026-09-26 定）：控制器里不放任何 `StateMachineBehaviour`，
  也不绑任何写 Hub 的曲线。"解释控制器结果时不看名字"这条老规矩因此还是干净的。
* ❌ **不提前扩展前缀**（`M/` 材质、`C/` 组件这些），三个够用。
* ❌ **不做跨角色共享词表**（2026-09-26 定）：名字跟着配置/预制件走，一个角色一份。
* ❌ **不用动画曲线写 Hub**（2026-09-26 定）：原因是"作者必须先知道下标"那一串问题。
* ❌ **不给 Hub 加长度 / 预留 API**（它是个纯存值的键值对列表；格是写的人按名字开的）。

## 7. 与既有设计的关系

* 这个类型是**新挂在角色上的** ⇒ 与"角色预制件零组件"那条老规矩**冲突**：
  那是**调试状态**的规矩（调试状态在 `Assets/HoFaceDebugSettings.json`，不进预制件），
  而 Hub 是**产品内容**、本来就必须在角色上。用户 2026-09-25 确认
  "可以接受角色 mod 制作时挂新脚本"（而且**带自定义脚本的角色 mod 打包这条路早就跑通了**），
  所以老规矩按"只约束调试状态"重新表述，不是被推翻。
  （⚠️ 控制器资产上现在**一个我们自己的组件都没有** —— 只有角色上那一个 Hub。）
* 求解节点现在的出口是**官方同形的 5 个 + `状态`**；语义值现在是**参数处理那条链的一个分支**
  （`参数处理.参数 → ┬→ 控制求解`、`└→ 写动态参数`），**不替换**既有的形状/骨骼出口。
  这与 [控制器输出范围](ANIMATOR_OUTPUT_PIPELINE_DESIGN.md) §6 的"保留现有五个 + 新增一份完整求值帧"
  **是同一个决定**：旧出口是同一帧结果的投影，**不各自重复求值**。
